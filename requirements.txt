# newest packages as of 2023-06-27
pandas==2.0.2 # Plotly express requires pandas to be installed, but Plotly doesn't install it as a dependency
plotly==5.15.0  # used to demonstrate that JS content is correctly rendered
pillow==9.5.0  # used to load image data
matplotlib==3.7.1 # used to show an image
nbconvert==7.6.0  # used to convert a notebook to (e.g.) an HTML file
ipykernel==6.23.3  # used to set a notebook environmen